import { IPrams } from '@/types/params.interface';
export default function RecipePage({ params, searchParams }: IPrams) {
    return (
        <div>
            <h1>recipe page</h1>
        </div>
    );
}
